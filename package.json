{
  "private": true,
  "scripts": {
    "docs": "yarn --cwd docs dev",
    "dev:vue": "cd vue-docs && yarn docs:dev --port 3001",
    "expo-managed": "yarn --cwd examples/expo-managed",
    "postinstall": "expo-yarn-workspaces check-workspace-dependencies"
  },
  "workspaces": [
    "docs",
    "examples/*",
    "packages/*",
    "vue-docs"
  ],
  "devDependencies": {
    "expo-yarn-workspaces": "^1.3.1"
  }
}
