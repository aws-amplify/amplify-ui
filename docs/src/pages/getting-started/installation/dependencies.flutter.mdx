import { Link, Alert, Text, Tabs, TabItem } from '@aws-amplify/ui-react';

Add the `amplify_authenticator` package with [pub](https://pub.dev/):

```shell
flutter pub add amplify_authenticator
```

This will add a dependencies like this to your package's pubspec.yaml

```yaml
dependencies:
  amplify_authenticator: ^1.0.0
```

<Alert variation="info" heading="Component Availability">
  <Text color="inherit">
    Currently only the{' '}
    <Link href={'/components/authenticator?platform=flutter'}>
      {' '}
      authenticator component{' '}
    </Link>{' '}
    is supported for flutter.
  </Text>
</Alert>
