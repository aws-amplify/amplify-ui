---
title: Installation
---

import Link from 'next/link';
import { Fragment } from '@/components/Fragment';

<Fragment platforms={['flutter']}>
  {({ platform }) => import(`./preqrequisite.${platform}.mdx`)}
</Fragment>

## Dependencies

### Packages

<Fragment>
  {({ platform }) => import(`./dependencies.${platform}.mdx`)}
</Fragment>

<Fragment platforms={['angular', 'react', 'vue']} useCommonJsContent>
  {() => import(`./styles.${platform}.mdx`)}
</Fragment>

<Fragment platforms={['angular', 'react', 'vue']} useCommonJsContent>
  {() => import(`./fonts.${platform}.mdx`)}
</Fragment>

<Fragment platforms={['angular', 'react', 'vue']}>
  {({ platform }) => import(`./troubleshooting.${platform}.mdx`)}
</Fragment>
