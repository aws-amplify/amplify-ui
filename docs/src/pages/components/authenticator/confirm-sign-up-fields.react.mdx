```jsx{11}
const components = {
  ConfirmSignUp: {
    FormFields() {
      return <Text>Please contact the adminstrator of your account to continue!</Text>;
    },
  },
};

export default function App() {
  return (
    <Authenticator components={components}>
      {({ signOut }) => <button onClick={signOut}>Sign out</button>}
    </Authenticator>
  );
}
```

Additionally, you can further customize the page by updating the [headers and footers](/components/authenticator?platform=react#headers--footers).
