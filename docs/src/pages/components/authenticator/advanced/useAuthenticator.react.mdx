### Access & Modify Auth State

`@aws-amplify/ui-react` ships with `useAuthenticator` hook that can be used to access, modify, and update Authenticator's auth state. Following is the list of variables that this hook provides per use case:
