### Access & Modify Auth State

`@aws-amplify/ui-react` ships with `useAuthenticator` hook that can be used to access, modify, and update Authenticator's auth state and form values. Following is the list of variables that this hook provides per use case:
