import { Fragment } from '@/components/Fragment';
import { FlutterAuthenticatorExample } from '@/components/FlutterAuthenticatorExample';

The `Authenticator` component adds complete [authentication flows](https://docs.amplify.aws/lib/auth/getting-started/q/platform/flutter)
to your application with minimal boilerplate.

<FlutterAuthenticatorExample id="flutter-authenticator-intro" />
