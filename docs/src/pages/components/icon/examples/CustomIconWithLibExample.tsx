import { Icon } from '@aws-amplify/ui-react';
import { DiJsBadge } from 'react-icons/di';

export const CustomIconWithLibExample = () => {
  return <Icon ariaLabel="Javascript" color="rebeccapurple" as={DiJsBadge} />;
};
