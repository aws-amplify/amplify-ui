import { Expander, ExpanderItem } from '@aws-amplify/ui-react';
import ReactMarkdown from 'react-markdown';
import remarkGfm from 'remark-gfm';

## Props

<Expander type="multiple" defaultValue={['ExpanderProps']}>
  
<ExpanderItem title="ExpanderProps" value="ExpanderProps">
    <ReactMarkdown
      remarkPlugins={[remarkGfm]}
      children={`
| Name | Type | Default | Description |
| ---- | ---- | ------- | ----------- |
| / | / | / | / |

      `}
    />

  </ExpanderItem>

<ExpanderItem title="BaseComponentProps" value="BaseComponentProps">
    <ReactMarkdown
      remarkPlugins={[remarkGfm]}
      children={`
| Name | Type | Default | Description |
| ---- | ---- | ------- | ----------- |
| / | / | / | / |

      `}
    />

  </ExpanderItem>

<ExpanderItem title="BaseStyleProps" value="BaseStyleProps">
    <ReactMarkdown
      remarkPlugins={[remarkGfm]}
      children={`
| Name | Type | Default | Description |
| ---- | ---- | ------- | ----------- |
| / | / | / | / |

      `}
    />

  </ExpanderItem>

<ExpanderItem title="RefAttributes" value="RefAttributes">
    <ReactMarkdown
      remarkPlugins={[remarkGfm]}
      children={`
| Name | Type | Default | Description |
| ---- | ---- | ------- | ----------- |
| ref | Ref&#60;unknown&#62; | / |  |

      `}
    />

  </ExpanderItem>

</Expander>
