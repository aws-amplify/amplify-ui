For Vue, import and add to your template the `Authenticator` component:

```js{2-3,7}
<script setup>
  import { Authenticator } from "@aws-amplify/ui-vue";
  import "@aws-amplify/ui-vue/styles.css";
</script>

<template>
  <authenticator />
</template>
```
