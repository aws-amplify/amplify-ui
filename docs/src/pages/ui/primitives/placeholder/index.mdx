---
title: Placeholder
---

import { Placeholder, View } from '@aws-amplify/ui-react';
import { PlaceholderDemo, TwoWays } from './demo';

## Installation

```
npm install @aws-amplify/ui-react
```

## Demo

<PlaceholderDemo />

## Usage

Import the Placeholder component and styles. There are two ways you can use the Placeholder component:

1. Use conditional rendering (e.g., ternary operator)
2. Pass your loading state to the `isLoaded` prop.

```jsx
import { Placeholder } from '@aws-amplify/ui-react';
import '@aws-amplify/ui-react/styles.css';

let isLoaded = false;

const PlaceholderDemo = () => (
  <View>
    {isLoaded ? 'First way' : <Placeholder />}
    <Placeholder isLoaded={isLoaded}>Second Way</Placeholder>
  </View>
);
```

<TwoWays />

### Placeholder sizes

Use the `size` prop to change the placeholder size. Available options are `small`, `medium`, and `large`.

```jsx
import { Placeholder } from '@aws-amplify/ui-react';
import '@aws-amplify/ui-react/styles.css';

<Placeholder size="small"></Placeholder>;
```

<Placeholder size="small"></Placeholder>

### CSS Styling
