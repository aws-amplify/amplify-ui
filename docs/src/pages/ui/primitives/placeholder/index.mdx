---
title: Placeholder
---

import { Placeholder, View, Flex } from '@aws-amplify/ui-react';
import { PlaceholderDemo, TwoWays } from './demo';

## Installation

```
npm install @aws-amplify/ui-react
```

## Demo

<PlaceholderDemo />

## Usage

Import the Placeholder component and styles.

```jsx
import { Placeholder, Flex } from '@aws-amplify/ui-react';
import '@aws-amplify/ui-react/styles.css';

<Flex direction="column">
  <Placeholder />
  <Placeholder />
  <Placeholder />
</Flex>
);
```

<Flex direction="column">
  <Placeholder />
  <Placeholder />
  <Placeholder />
</Flex>

### There are two ways you can use the Placeholder component:

1. Pass your loading state to the `isLoaded` prop.
2. Use conditional rendering (e.g., ternary operator)

```jsx
let isLoaded = false;

<View>
  <Placeholder isLoaded={isLoaded}>First way</Placeholder>
  {isLoaded ? 'Second way' : <Placeholder />}
</View>
);
```

<TwoWays />

### Placeholder sizes

Use the `size` prop to change the placeholder size. Available options are `small`, `medium`, and `large`.

```jsx
import { Placeholder } from '@aws-amplify/ui-react';
import '@aws-amplify/ui-react/styles.css';

<Placeholder size="small" />
<Placeholder size="medium" />
<Placeholder size="large" />
```

<Flex direction="column">
  <Placeholder size="small" />
  <Placeholder size="medium" />
  <Placeholder size="large" />
</Flex>

### CSS Styling

Set the starting and ending colors using a custom CSS class with CSS variables:

```css
/* styles.css */
.custom-placeholder {
  --start-color: purple;
  --end-color: blue;
}
```

```jsx
import './styles.css';

<Placeholder className="custom-placeholder" />;
```

<Placeholder className="custom-placeholder" />

### Override Amplify styling using props

Use style props to style an instance of `<Placeholder>`

```jsx
<Placeholder height="123px" width="50%" />
```

<Placeholder height="123px" width="50%" />
