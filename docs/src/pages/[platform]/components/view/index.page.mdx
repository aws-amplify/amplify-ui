---
title: View
metaTitle: View
description: View is the container component within which all other components live.
metaDescription: View is the container component within which all other components live.
reactSource: packages/react/src/primitives/View/View.tsx
supportedFrameworks: react
---

import { Fragment } from '@/components/Fragment';

<Fragment>{({ platform }) => import(`./${platform}.mdx`)}</Fragment>
