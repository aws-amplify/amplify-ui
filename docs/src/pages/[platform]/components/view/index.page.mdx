---
title: View
metaTitle: View
description: View is the foundational component for Amplify UI. View is a container that maps directly to <div> by default, and can be configured to render any HTML element.
metaDescription: View is the foundational component for Amplify UI. View is a container that maps directly to <div> by default, and can be configured to render any HTML element.
reactSource: packages/react/src/primitives/View/View.tsx
supportedFrameworks: react
---

import { Fragment } from '@/components/Fragment';

<Fragment>{({ platform }) => import(`./${platform}.mdx`)}</Fragment>
