---
title: Component Style Attributes
metaDescription: 'CSS Style Attributes that are available on every amplify-ui component'
supportedFrameworks: react|angular|vue
---

import { Example, ExampleCode } from '@/components/Example';

These style attributes are available on all amplify-ui primitive components and can be used to modify the corresponding styles. This allows users to quickly make style adjustments without having to go through css or add a theme object.

### Spacing
