import { Fragment } from '@/components/Fragment';
import { 
  TokensSizeSpace,
  TokensSizeBorderWidths,
  TokensSizeRadii } from '@/components/Theming/TokenGroups';

## Space

<Fragment>{({ platform }) => import(`./spaceUsage.${platform}.mdx`)}</Fragment>

<TokensSizeSpace />

## Border Widths

<Fragment>{({ platform }) => import(`./borderWidthUsage.${platform}.mdx`)}</Fragment>

<TokensSizeBorderWidths />

## Radii

<Fragment>{({ platform }) => import(`./radiiUsage.${platform}.mdx`)}</Fragment>

<TokensSizeRadii />
