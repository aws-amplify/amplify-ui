---
title: Responsive Design
metaTitle: Responsive Design
metaDescription: Responsive styling is supported out of the box using our default breakpoints. Our responsive support uses a mobile first approach, so @media(min-width) rules are used for all breakpoints.
supportedFrameworks: react|angular|vue
---

import { Fragment } from '@/components/Fragment';

<Fragment useCommonWebContent>
  {({ platform }) => import(`./${platform}.mdx`)}
</Fragment>
