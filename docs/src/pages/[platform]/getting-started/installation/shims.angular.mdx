### Global and Process Shim

Angular does not include shims for `global` or `process` which are required to use the `aws-amplify` library.
See [instructions to install polyfills](./troubleshooting#global-and-process-shim).
