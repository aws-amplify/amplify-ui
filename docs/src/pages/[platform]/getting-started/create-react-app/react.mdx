import { Link, Tabs, TabItem } from '@aws-amplify/ui-react';
import { Example, ExampleCode } from '@/components/Example';
import { BasicExampleDemo, CompleteExampleDemo } from './examples';

### TESTING 123

Is there a way to make this terminal code snippet copy/pastable?

```shell
npx create-react-app amplify-ui-demo
```

<Link href="https://create-react-app.dev" isExternal>
  Link CRA docs
</Link>

`cd` into the folder and install the Amplify UI React package:

<Tabs>
<TabItem title="npm">

```shell
npm install aws-amplify @aws-amplify/ui-react
```

</TabItem>
<TabItem title="yarn">

```shell
yarn add aws-amplify @aws-amplify/ui-react
```

</TabItem>
</Tabs>

Open up the project in your IDE and replace the contents of the `App.js` file with the code below. When you start the app, you should see a basic shopping card component.

<Example>
  <ExampleCode>
  
```tsx file=./examples/BasicExampleCode.tsx

````

  </ExampleCode>
  <BasicExampleDemo />
</Example>

### More complex

If you’d like to play with a more comprehensive demo of the Amplify UI library, create a separate file under the `src` folder named `paintings.js`, and copy/paste this code into it:

  <ExampleCode>

```tsx file=./examples/paintings.ts

````

  </ExampleCode>

Then, replace the contents of `App.js` with the code below, and restart the app.

<Example>
  <ExampleCode>

```tsx file=./examples/CompleteExampleCode.tsx

```

  </ExampleCode>
  <CompleteExampleDemo />
</Example>
