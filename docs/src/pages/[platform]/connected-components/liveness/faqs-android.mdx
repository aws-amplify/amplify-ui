### (Android-specific) When creating a backend using a REST API, seeing compilation errors relating to `com.amazonaws.mobileconnectors.apigateway.annotation`

As per our [documentation](https://docs.amplify.aws/lib/restapi/getting-started/q/platform/android/#configure-api), these autogenerated files can be safely deleted — this should be a one-time fix after `amplify add api` and `amplify push`.