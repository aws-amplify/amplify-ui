import { Image, View } from '@aws-amplify/ui-react';

```kotlin
val authenticatorState = rememberAuthenticatorState(
    initialStep = AuthenticatorStep.SignIn // Type checking ensures you can only pass SignIn, SignUp, and ResetPassword here
)

Authenticator(state = authenticatorState) {
    // ...
}
```

<View
    width="100%"
    textAlign="center"
>
    <Image alt="Sign in with Amplify Android Authenticator" width="33%" src="/android/authenticator/sign-in.png" />
</View>