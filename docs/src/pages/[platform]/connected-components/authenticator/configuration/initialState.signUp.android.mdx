import { Image } from '@aws-amplify/ui-react';

```kotlin
val authenticatorState = rememberAuthenticatorState(
    initialStep = AuthenticatorStep.SignUp // Type checking ensures you can only pass SignIn, SignUp, and ResetPassword here
)

Authenticator(state = authenticatorState) {
    // ...
}
```

<Image alt="Sign up with Amplify Android Authenticator" width="33%" src="/android/authenticator/sign-up.png" />