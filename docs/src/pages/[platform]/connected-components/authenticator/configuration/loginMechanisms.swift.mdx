import { Alert } from '@aws-amplify/ui-react';

<Alert role="none" heading="Zero Configuration">

Whether the user is able to sign in with their username, phone number or email is [automatically inferred](/connected-components/authenticator#step-1-configure-backend) from the current Amplify configuration.

This determines which login mechanism the Authenticator for Swift uses, no other configuration is supported.

</Alert>
