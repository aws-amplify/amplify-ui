---
title: Configuration
metaTitle: Configuration
description: How to setup and configure your Authenticator component.
supportedFrameworks: all
---

import { Fragment } from '@/components/Fragment';
import { Alert, Tabs, TabItem } from '@aws-amplify/ui-react';
import { FRAMEWORKS } from '@/data/frameworks';
import { getCustomStaticPath } from "@/utils/getCustomStaticPath";

export async function getStaticPaths() {
  return getCustomStaticPath(frontmatter.supportedFrameworks);
}

{/*  `getStaticProps` is required to prevent "Error: getStaticPaths was added without a getStaticProps. Without getStaticProps, getStaticPaths does nothing" */}

export async function getStaticProps() {
  return { props: {} }
}

<Alert role="none" heading="Wait!">
  Did you follow the [quick start instructions](./) to set up the Authenticator first?
</Alert>

## Initial State

By default, unauthenticated users are redirected to the Sign In flow. You can explicitly redirect to Sign Up or Reset Password:

<Tabs>
  <TabItem title="Sign In">
    <Fragment>
      {({ platform }) => 
        platform !== 'react-native' ? import(`./initialState.signIn.${platform}.mdx`) : Promise.resolve(() => null)
      }
    </Fragment>
    <Fragment useCommonWebContent>
      {({ platform }) =>
        platform !== 'react-native' ? import(`./initialState.signIn.example.${platform}.mdx`) : Promise.resolve(() => null)
      }
    </Fragment>
  </TabItem>
  <TabItem title="Sign Up">
    <Fragment>
      {({ platform }) => import(`./initialState.signUp.${platform}.mdx`)}
    </Fragment>
    <Fragment useCommonWebContent>
      {({ platform }) =>
        platform !== 'react-native' ? import(`./initialState.signUp.example.${platform}.mdx`) : Promise.resolve(() => null)
      }
    </Fragment>
  </TabItem>
  <TabItem title="Reset Password">
    <Fragment>
      {({ platform }) => import(`./initialState.resetPassword.${platform}.mdx`)}
    </Fragment>
    <Fragment useCommonWebContent>
      {({ platform }) =>
        platform !== 'react-native' ? import(`./initialState.resetPassword.example.${platform}.mdx`) : Promise.resolve(() => null)
      }
    </Fragment>
  </TabItem>
</Tabs>

## Login Mechanisms

<Fragment useCommonWebContent>
  {({ platform }) => platform !== 'react-native' ? import(`./loginMechanisms.${platform}.mdx`) : Promise.resolve(() => null)}
</Fragment>

## Sign Up Attributes

<Alert role="none" heading="Zero Configuration">

The Authenticator [automatically infers](#zero-configuration) `signUpAttributes` from `amplify pull`,
but can be explicitly defined as seen below.

</Alert>

The Authenticator automatically renders _most_ [Cognito User Pools attributes](https://docs.aws.amazon.com/cognito/latest/developerguide/user-pool-settings-attributes.html),
with the exception of `address`, `gender`, `locale`, `picture`, `updated_at`, and `zoneinfo`. Because these are often app-specific, they can be customized via [Sign Up fields](customization#sign-up-fields).

<Tabs>
  <TabItem title="Verification Attributes">
    By default, the Authenticator will still require any attributes required for
    verification, such as `email`, _even if `signUpAttributes` is empty_:
    <Fragment>
      {({ platform }) => platform !== 'react-native' ? import(`./signUpAttributes.default.${platform}.mdx`) : Promise.resolve(() => null)}
    </Fragment>
    <Fragment useCommonWebContent>
      {({ platform }) =>
        platform !== 'react-native' ? import(`./signUpAttributes.default.example.${platform}.mdx`) : Promise.resolve(() => null)
      }
    </Fragment>
  </TabItem>
  <TabItem title="All Attributes">
    <Fragment>
      {({ platform }) => import(`./signUpAttributes.all.${platform}.mdx`)}
    </Fragment>
    <Fragment useCommonWebContent>
      {({ platform }) =>
        platform !== 'react-native' ? import(`./signUpAttributes.all.example.${platform}.mdx`) : Promise.resolve(() => null)
      }
    </Fragment>
  </TabItem>
</Tabs>

## Social Providers

<Fragment useCommonWebContent>
  {({ platform }) => platform !== 'react-native' ? import(`./socialProviders.${platform}.mdx`) : Promise.resolve(() => null)}
</Fragment>

<Fragment useCommonWebContent platforms={['web']}>
  {({ platform }) => import(`./variation.${platform}.mdx`)}
</Fragment>

<Fragment useCommonWebContent platforms={['web']}>
  {({ platform }) => import(`./hidesignup.${platform}.mdx`)}
</Fragment>
