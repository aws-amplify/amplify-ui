---
title: Customization
metaTitle: Customization
description: Override and customize your Authenticator.
supportedFrameworks: all
---

import { Fragment } from '@/components/Fragment';
import { FRAMEWORKS } from '@/data/frameworks';
import { getCustomStaticPath } from "@/utils/getCustomStaticPath";

export async function getStaticPaths() {
  return getCustomStaticPath(frontmatter.supportedFrameworks);
}

{/*  `getStaticProps` is required to prevent "Error: getStaticPaths was added without a getStaticProps. Without getStaticProps, getStaticPaths does nothing" */}

export async function getStaticProps() {
  return { props: {} }
}

<Fragment platforms={['flutter']}>
  {({ platform }) => import(`./customization.routing.${platform}.mdx`)}
</Fragment>

<Fragment useCommonWebContent platforms={['web']}>
  {({ platform }) =>
    import(`./customization.headers-and-footers.${platform}.mdx`)
  }
</Fragment>

<Fragment useCommonWebContent platforms={['web']}>
  {({ platform }) =>
    import(`./customization.override-function-calls.${platform}.mdx`)
  }
</Fragment>

## Internationalization (I18n)

<Fragment useCommonWebContent>
  {({ platform }) => platform !== 'react-native' ? import(`./customization.i18n.${platform}.mdx`) : Promise.resolve(() => null)}
</Fragment>

## Labels & Text

<Fragment useCommonWebContent>
  {({ platform }) => platform !== 'react-native' ? import(`./customization.labels-and-text.${platform}.mdx`) : Promise.resolve(() => null)}
</Fragment>

## Sign Up Fields

<Fragment useCommonWebContent>
  {({ platform }) => platform !== 'react-native' ? import(`./sign-up-fields.${platform}.mdx`) : Promise.resolve(() => null)}
</Fragment>

## Force New Password Fields

The following example customizes the Force New Password screen by:

- Re-using the default Force New Password form fields
- Appending a custom "Zone Info" text field

<Fragment>
  {({ platform }) => platform !== 'react-native' ? import(`./force-new-password-fields.${platform}.mdx`) : Promise.resolve(() => null)}
</Fragment>

<Fragment useCommonWebContent platforms={['web']}>
  {({ platform }) => import(`./formfields/form-fields.${platform}.mdx`)}
</Fragment>

<Fragment useCommonWebContent>
  {({ platform }) => platform !== 'react-native' ? import(`./customization.styling.${platform}.mdx`) : Promise.resolve(() => null)}
</Fragment>

<Fragment platforms={['flutter']}>
  {({ platform }) =>
    import(`./customization.full-ui-customization.${platform}.mdx`)
  }
</Fragment>
