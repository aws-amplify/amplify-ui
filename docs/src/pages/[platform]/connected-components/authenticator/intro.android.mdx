import { Image } from '@aws-amplify/ui-react';
import { Fragment } from '@/components/Fragment';
import { FlutterAuthenticatorExample } from '@/components/FlutterAuthenticatorExample';

The `Authenticator` component adds complete [authentication flows](https://docs.amplify.aws/lib/auth/getting-started/q/platform/android) to your application with minimal boilerplate.

<Image
  alt="Example screenshot of the Android Authenticator."
  height={{ base: '25rem', medium: '25rem' }}
  display="block"
  objectFit="contain"
  style={{ margin: '0 auto' }}
  src="/android-authenticator-preview.png"
/>
