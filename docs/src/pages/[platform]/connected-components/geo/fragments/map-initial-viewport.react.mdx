```tsx
import { MapView } from '@aws-amplify/ui-react-geo';

export default function InitialViewport() {
  return (
    <MapView
      initialViewState={{
        latitude: 37.8,
        longitude: -122.4,
        zoom: 14,
      }}
    />
  );
}
```
