// Add a comment explaining your reasoning
// We have to use 'any' as well (try it out on Alert to see why)
// The key is the component's display name
// The value is the component demo's props
export const demoState = new Map<string, any>();
