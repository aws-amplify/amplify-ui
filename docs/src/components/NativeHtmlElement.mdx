import { Link } from '@aws-amplify/ui-react';
import { Fragment } from '@/components/Fragment';

### Standard HTML attributes

The <code>{props.component}</code> will accept any of the native HTML properties that a <code>{props.element}</code> element accepts.
Native <code>{props.element}</code> properties can be found at <Link isExternal="true" href={`https://developer.mozilla.org/en-US/docs/Web/HTML/Element/${props.link||props.component}#attributes`}>{props.link||props.component}</Link>

<>{props.children}</>
