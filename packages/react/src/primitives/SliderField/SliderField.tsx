import { FieldGroup } from '../FieldGroup';
import { Flex } from '../Flex';
import { SliderFieldProps } from '../types/sliderField';
import { Primitive } from '../types/view';

export const SliderField: Primitive<SliderFieldProps, 'input'> = () => {
  return <Flex></Flex>;
};
