import { StyleSheet } from 'react-native';
import { IconButtonStyles } from './types';

export const styles: IconButtonStyles = StyleSheet.create({
  pressed: { opacity: 0.6 },
  text: { alignSelf: 'center' },
});
