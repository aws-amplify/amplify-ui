export const DEFAULTS = {
  ignoreKeys: ['original'],
};

export const OPTIONS = {
  openingCharacter: '{',
  closingCharacter: '}',
  separator: '.',
};

export const REFERENCE_REGEX = /\{([^}]+)\}/g;
