{
  "private": true,
  "name": "environments",
  "version": "0.0.1",
  "scripts": {
    "auth": "yarn --cwd auth/",
    "datastore": "yarn --cwd datastore/",
    "geo": "yarn --cwd geo/",
    "storage": "yarn --cwd storage/"
  }
}
