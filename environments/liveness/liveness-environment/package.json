{
  "private": true,
  "name": "@aws-amplify/ui-liveness-environment",
  "version": "0.0.1",
  "scripts": {
    "pull": "amplify pull --appId d2pnskzbmchcgk --envName dev",
    "generate": "echo 'Not available yet. Generating a placeholder file for now...' && touch amplify_outputs.json && echo '{}' > amplify_outputs.json"
  }
}
