<!--
  Root landing route for the examples app. SvelteKit's `vite preview` server is
  route-aware and responds 404 to any path that matches no route, so `/` needs a
  route to return 2xx for E2E server-readiness checks — the exercised demos
  themselves live under `/ui/components/authenticator/...`.
-->
<h1>Amplify UI Svelte examples</h1>
