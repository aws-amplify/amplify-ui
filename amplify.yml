version: 1
applications:
  - frontend:
      phases:
        preBuild:
          commands:
            - nvm use
            - node -v
            - yarn install
        build:
          commands:
            - nvm use
            - node -v
            - yarn build
      artifacts:
        baseDirectory: out
        files:
          - "**/*"
      cache:
        paths:
          - node_modules/**/*
    appRoot: docs
